name: limelight
description: The number one recipe app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.0.0'

dependencies:
  flutter:
    sdk: flutter
  fluttericon:
  google_fonts:
  provider:
  shared_preferences:

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter
  test:
  test_api:

flutter:
  uses-material-design: true

  assets:
    - assets/
